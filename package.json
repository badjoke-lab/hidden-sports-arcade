{
  "name": "hidden-sports-arcade",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A lightweight static browser arcade for simplified hidden and lesser-known sports.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "check:routes": "node scripts/check-routes-render.mjs"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.2.4"
  }
}
